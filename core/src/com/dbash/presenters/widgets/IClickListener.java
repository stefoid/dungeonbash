package com.dbash.presenters.widgets;

// Pass an anon class that implements this interface to anything with onClick(), for closure like behavior
public interface IClickListener {

	public void processClick();

}